var React = require('react');

/**
 * Just the footer at the bottom of the app
 */
class Footer extends React.Component {
  render() {
    return (
      <footer>Hello World!</footer>
    );
  }
}

module.exports = Footer;
